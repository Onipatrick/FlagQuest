<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flatearthstudios.flagquest"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- android:screenOrientation="portrait" locks the orientation in portrait mode -->
        <activity
            android:name="com.flatearthstudios.flagquest.Main"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".North" android:screenOrientation="portrait"></activity>
        <activity android:name=".South" android:screenOrientation="portrait"></activity>
        <activity android:name=".Europe" android:screenOrientation="portrait"></activity>
    </application>

</manifest>